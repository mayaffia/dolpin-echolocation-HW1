# 🚀 Как запустить бэкенд

## Вариант 1: Быстрый запуск (рекомендуется)

### macOS/Linux:

```bash
cd ifraimova/gui_app
chmod +x run.sh
./run.sh
```

### Windows:

```cmd
cd ifraimova\gui_app
run.bat
```

## Вариант 2: Ручной запуск

### Шаг 1: Перейти в папку

```bash
cd ifraimova/gui_app
```

### Шаг 2: Создать виртуальное окружение (первый раз)

```bash
python3 -m venv venv
```

### Шаг 3: Активировать виртуальное окружение

**macOS/Linux:**

```bash
source venv/bin/activate
```

**Windows:**

```cmd
venv\Scripts\activate
```

### Шаг 4: Установить зависимости (первый раз)

```bash
pip install -r requirements.txt
```

### Шаг 5: Запустить сервер

```bash
cd backend
python server.py
```

## ✅ Проверка

После запуска вы должны увидеть:

```
======================================================================
Dolphin Echolocation GUI Server
======================================================================
Server starting on http://localhost:5000
React frontend should connect to this server
======================================================================
```

## 🌐 Открыть в браузере

После запуска бэкенда откройте в браузере:

```
http://localhost:5000
```

## ❌ Если не работает

### Проблема: ModuleNotFoundError: No module named 'dolphin'

**Решение:** Убедитесь, что структура папок правильная:

```
HW1/
└── ifraimova/
    ├── dolphin.py          ← должен быть здесь!
    └── gui_app/
        └── backend/
            └── server.py
```

### Проблема: Port 5000 already in use

**Решение:** Измените порт в файле `backend/server.py` (последняя строка):

```python
app.run(debug=True, host='0.0.0.0', port=5001)  # измените на 5001
```

### Проблема: pip install не работает

**Решение:** Обновите pip:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

## 📦 Что установится

Бэкенд использует следующие библиотеки:

- flask >= 3.0.0
- flask-cors >= 4.0.0
- numpy >= 1.24.0

## 🔄 Перезапуск

Чтобы перезапустить сервер:

1. Нажмите `Ctrl+C` в терминале
2. Запустите снова: `python server.py`

## 📝 Логи

Все запросы будут отображаться в терминале, где запущен сервер.
