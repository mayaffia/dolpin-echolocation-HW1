# 🚀 Быстрый старт

## Запуск приложения

### 1. Запустите бэкенд (Python Flask)

```bash
cd gui_app
source venv/bin/activate  # На Windows: venv\Scripts\activate
cd backend
python server.py
```

Бэкенд запустится на `http://localhost:5000`

### 2. Запустите фронтенд (React)

В новом терминале:

```bash
cd gui_app/react-frontend
npm install  # Только первый раз
npm start
```

Фронтенд откроется автоматически в браузере на `http://localhost:3000`

## Первая установка

### Бэкенд:

```bash
cd gui_app
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Фронтенд:

```bash
cd gui_app/react-frontend
npm install
```

## Использование

1. **Выберите функцию** (Sphere, Rastrigin, Rosenbrock)
2. **Настройте параметры**:
   - Размерность (2-20)
   - Размер популяции (5-100)
   - Максимум итераций (10-500)
3. **Нажмите "Инициализировать"** 🚀
4. **Выберите 2 переменные** для визуализации
5. **Используйте кнопки управления**:
   - ▶️ **Старт** - непрерывное выполнение
   - ⏭️ **Шаг** - одна итерация
   - ⏹️ **Стоп** - пауза
   - 🔄 **Сброс** - начать заново

## Возможности

✅ Интерактивная 2D визуализация функций
✅ Пошаговое выполнение алгоритма
✅ Отображение позиций агентов с номерами
✅ Визуализация лучшего решения (желтая звезда)
✅ Траектории движения агентов
✅ График сходимости в реальном времени
✅ Настройка всех параметров алгоритма
✅ Красивый современный интерфейс

## Требования

- Python 3.8+
- Node.js 14+
- Современный браузер (Chrome, Firefox, Safari, Edge)

## Порты

- Backend: `http://localhost:5000`
- Frontend: `http://localhost:3000`

Убедитесь, что эти порты свободны!
