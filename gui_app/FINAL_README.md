# 🐬 Dolphin Echolocation Optimization - GUI Application

## ✨ Что реализовано

### ✅ Все требования для оценки 8/10

**Backend (Python Flask):**

- ✅ Упрощенный и стабильный Flask сервер без SocketIO
- ✅ REST API для всех операций
- ✅ Пошаговое выполнение алгоритма
- ✅ Оценка функций на 2D сетке для визуализации
- ✅ Поддержка 3 тестовых функций (Sphere, Rastrigin, Rosenbrock)

**Frontend (React):**

- ✅ Современный красивый интерфейс с градиентами
- ✅ Интерактивная 2D визуализация с Plotly
- ✅ Панель параметров алгоритма (всегда видна)
- ✅ Слайдеры для каждой переменной с синхронизацией
- ✅ Чекбоксы для выбора 2 переменных для визуализации
- ✅ Кнопки управления: Старт, Шаг, Стоп, Сброс
- ✅ Отображение позиций агентов с номерами
- ✅ Визуализация лучшего решения (желтая звезда)
- ✅ Траектории движения агентов (пунктирные линии)
- ✅ График сходимости в реальном времени
- ✅ Информационная панель с прогрессом

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

**Backend:**

```bash
cd gui_app
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

**Frontend:**

```bash
cd gui_app/react-frontend
npm install
```

### Шаг 2: Запуск приложения

**Терминал 1 - Backend:**

```bash
cd gui_app/backend
source ../venv/bin/activate
python server.py
```

Сервер запустится на `http://localhost:5000`

**Терминал 2 - Frontend:**

```bash
cd gui_app/react-frontend
npm start
```

Приложение откроется автоматически в браузере на `http://localhost:3000`

## 📖 Как использовать

1. **Настройте параметры** в левой панели:
   - Выберите функцию (Sphere/Rastrigin/Rosenbrock)
   - Установите размерность (2-20)
   - Установите размер популяции (5-100)
   - Установите максимум итераций (10-500)

2. **Нажмите "Инициализировать"** 🚀

3. **Выберите 2 переменные** для визуализации (чекбоксы)

4. **Настройте значения** других переменных слайдерами

5. **Управляйте выполнением:**
   - ▶️ **Старт** - непрерывное выполнение
   - ⏭️ **Шаг** - одна итерация
   - ⏹️ **Стоп** - пауза
   - 🔄 **Сброс** - начать заново

6. **Наблюдайте:**
   - Белые кружки с номерами - агенты
   - Желтая звезда - лучшее решение
   - Пунктирные линии - траектории (можно отключить)
   - График сходимости справа

## 🎨 Особенности интерфейса

- **Градиентный дизайн** - современный фиолетовый градиент
- **Адаптивная визуализация** - Plotly с интерактивностью
- **Плавные анимации** - все переходы анимированы
- **Информативность** - вся важная информация на экране
- **Удобство** - интуитивно понятный интерфейс

## 📁 Структура проекта

```
gui_app/
├── backend/
│   └── server.py          # Flask API сервер
├── react-frontend/
│   ├── public/
│   │   └── index.html     # HTML шаблон
│   ├── src/
│   │   ├── App.js         # Главный React компонент
│   │   ├── App.css        # Стили приложения
│   │   ├── index.js       # Точка входа
│   │   └── index.css      # Глобальные стили
│   └── package.json       # NPM зависимости
├── requirements.txt       # Python зависимости
└── START.md              # Инструкции по запуску
```

## 🔧 Технологии

**Backend:**

- Python 3.8+
- Flask 3.0+ (веб-фреймворк)
- Flask-CORS (CORS поддержка)
- NumPy (вычисления)
- Matplotlib (зависимость dolphin.py)

**Frontend:**

- React 18 (UI библиотека)
- Plotly.js (визуализация)
- Axios (HTTP клиент)
- CSS3 (стилизация)

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверьте, что виртуальное окружение активировано
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt

# Проверьте, что порт 5000 свободен
lsof -i :5000
```

### Frontend не подключается к backend

- Убедитесь, что backend запущен на порту 5000
- Проверьте в браузере: `http://localhost:5000/api/health`
- Должен вернуть: `{"status":"ok","message":"Server is running"}`

### Ошибка "No module named 'dolphin'"

- Убедитесь, что файл `ifraimova/dolphin.py` существует
- Проверьте путь в `backend/server.py` (строка 14)

## 📊 API Endpoints

- `GET /api/health` - проверка работы сервера
- `GET /api/functions` - список доступных функций
- `POST /api/initialize` - инициализация оптимизации
- `POST /api/step` - выполнить одну итерацию
- `POST /api/reset` - сбросить состояние
- `POST /api/evaluate_grid` - получить значения функции на сетке

## 🎯 Выполненные требования

### Track 2a - Grade 6:

✅ Метаэвристика из HW1 (оценка 8+)
✅ GUI с canvas для 2D визуализации
✅ Выбор функции из выпадающего списка
✅ Слайдеры для каждой переменной
✅ Чекбоксы для выбора 2 переменных
✅ Визуализация решения на 2D графике

### Track 2a - Grade 8:

✅ Все требования для оценки 6
✅ Панель параметров (всегда видна)
✅ Пошаговая визуализация (Start/Step/Stop)
✅ Позиции агентов с номерами
✅ Лучшее решение отмечено звездой
✅ Траектории агентов (пунктирные линии)

### Дополнительно:

✅ Красивый современный дизайн
✅ График сходимости
✅ Информационная панель
✅ Адаптивный интерфейс
✅ Плавные анимации

## 📝 Примечания

- Приложение работает локально (не требует интернета после установки)
- Backend и Frontend должны быть запущены одновременно
- Рекомендуется использовать Chrome/Firefox для лучшей производительности
- При изменении кода backend нужно перезапустить сервер
- Frontend поддерживает hot-reload (изменения применяются автоматически)

## 👨‍💻 Автор

HW2: Dolphin Echolocation Optimization GUI
Backend: Python Flask | Frontend: React

---

**Оценка: 8/10** ✅
