# ✅ ПРАВИЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ ЗАПУСКА

## Вы сейчас в папке backend, нужно вернуться назад!

### Текущая ситуация:

```
Вы здесь: /Users/mayaffia/Desktop/HW1/ifraimova/gui_app/backend
requirements.txt находится: /Users/mayaffia/Desktop/HW1/ifraimova/gui_app/requirements.txt
```

## 🚀 ПРАВИЛЬНЫЕ КОМАНДЫ:

### 1. Вернитесь в папку gui_app:

```bash
cd ..
```

### 2. Активируйте виртуальное окружение (если ещё не активировано):

```bash
source venv/bin/activate
```

### 3. Установите зависимости:

```bash
pip install -r requirements.txt
```

### 4. Перейдите в backend:

```bash
cd backend
```

### 5. Запустите сервер:

```bash
python server.py
```

## ⚡ ИЛИ ВСЁ СРАЗУ (из папки backend):

```bash
cd .. && source venv/bin/activate && pip install -r requirements.txt && cd backend && python server.py
```

## 📝 Полная последовательность с нуля:

```bash
# Из любого места перейти в gui_app
cd /Users/mayaffia/Desktop/HW1/ifraimova/gui_app

# Активировать venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Перейти в backend
cd backend

# Запустить сервер
python server.py
```

## ✅ После запуска вы увидите:

```
======================================================================
Dolphin Echolocation GUI Server
======================================================================
Server starting on http://localhost:5000
React frontend should connect to this server
======================================================================
```

Откройте браузер: **http://localhost:5000**
