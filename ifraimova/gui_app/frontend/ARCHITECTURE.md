# Frontend Architecture

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── ParametersPanel.js      # Панель параметров алгоритма
│   ├── VariableSelector.js     # Выбор переменных для визуализации
│   ├── VariableSliders.js      # Слайдеры значений переменных
│   ├── VisualizationPanel.js   # 2D визуализация
│   ├── ControlPanel.js         # Кнопки управления (Start/Step/Stop/Reset)
│   └── InfoPanel.js            # Информационная панель (прогресс, результаты)
│
├── hooks/               # Custom React hooks
│   └── useOptimization.js      # Хук для управления оптимизацией
│
├── services/            # API сервисы
│   └── api.js                  # Централизованные запросы к бэкенду
│
├── utils/               # Утилиты
│   └── plotBuilder.js          # Построение графиков Plotly
│
├── App.js               # Главный компонент
├── App.css              # Стили
├── index.js             # Точка входа
└── index.css            # Глобальные стили
```

## 🏗️ Архитектурные принципы

### 1. Разделение ответственности (Separation of Concerns)

- **Components** - только UI и взаимодействие с пользователем
- **Hooks** - бизнес-логика и управление состоянием
- **Services** - взаимодействие с API
- **Utils** - вспомогательные функции

### 2. Компонентная архитектура

Каждый компонент отвечает за одну часть интерфейса:

- `ParametersPanel` - настройка параметров алгоритма
- `VariableSelector` - выбор переменных для 2D визуализации
- `VariableSliders` - управление значениями переменных
- `VisualizationPanel` - отображение 2D графика
- `ControlPanel` - кнопки управления процессом
- `InfoPanel` - отображение прогресса и результатов

### 3. Custom Hooks

`useOptimization` - инкапсулирует всю логику работы с оптимизацией:

- Управление состоянием (state, isRunning, agentHistory)
- Методы: initialize, step, start, stop, reset
- Автоматическое выполнение шагов

### 4. API Service Layer

Централизованное управление запросами к бэкенду:

- `getFunctions()` - получить список функций
- `initializeOptimization()` - инициализация
- `stepOptimization()` - выполнить шаг
- `resetOptimization()` - сброс
- `evaluateGrid()` - получить данные для визуализации

### 5. Утилиты

`plotBuilder.js` - построение данных для Plotly:

- `buildPlotData()` - создание traces (контур, агенты, траектории)
- `buildPlotLayout()` - настройка layout графика

## 🔄 Поток данных

```
User Action
    ↓
Component (UI)
    ↓
Hook (Business Logic)
    ↓
Service (API Call)
    ↓
Backend
    ↓
Service (Response)
    ↓
Hook (State Update)
    ↓
Component (Re-render)
```

## 📊 Управление состоянием

### Локальное состояние (useState)

- Параметры UI (dimension, populationSize, etc.)
- Выбранные переменные (selectedVars)
- Значения слайдеров (sliderValues)
- Данные графика (plotData)

### Хук состояния (useOptimization)

- Состояние оптимизации (state)
- Флаг выполнения (isRunning)
- История позиций (agentHistory)

## 🎨 Преимущества новой структуры

1. **Модульность** - каждый компонент независим
2. **Переиспользуемость** - компоненты можно использовать повторно
3. **Тестируемость** - легко писать unit-тесты
4. **Читаемость** - код разбит на логические части
5. **Масштабируемость** - легко добавлять новые функции
6. **Поддерживаемость** - проще находить и исправлять баги

## 🔧 Как добавить новый компонент

1. Создайте файл в `src/components/`
2. Экспортируйте компонент
3. Импортируйте в `App.js`
4. Используйте в JSX

Пример:

```javascript
// src/components/NewComponent.js
import React from "react";

const NewComponent = ({ prop1, prop2 }) => {
  return <div>...</div>;
};

export default NewComponent;
```

## 📝 Соглашения о коде

- Компоненты - PascalCase (ParametersPanel)
- Функции - camelCase (handleInitialize)
- Константы - UPPER_CASE (API_URL)
- Файлы компонентов - PascalCase.js
- Файлы утилит - camelCase.js

## 🚀 Запуск

```bash
cd ifraimova/gui_app/react-frontend
npm install
npm start
```

Приложение откроется на `http://localhost:3000`
